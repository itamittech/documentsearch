spring:
  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=100,expireAfterWrite=5m

elasticsearch:
  host: localhost
  port: 9200

logging:
  level:
    com.enterprise.docsearch: DEBUG
    org.springframework.web: INFO